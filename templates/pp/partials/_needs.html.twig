{% if presentation.needs | length > 0 %}

<h3 class="font-weight-bolder">Participer</h3>

<div class="container-fluid table-responsive mb-2">
    
    <table class="table table-hover table-bordered">
        
        <tbody>

            {% for need in presentation.needs %}

                {% set showPath = path('need_show', {'id': need.id, 'slug': presentation.slug}) %}
        
                <tr class="cursor-pointer h5 font-weight-bold">

                    <td>                                            
                        <a href="{{ showPath }}" class="text-body text-decoration-none"><span class="arrowCar rightArrow">► </span>{{ need.title }}
                        </a>
                    </td>

                </tr>

                
            {% endfor %}

        </tbody>
         
    </table>

</div>

{% endif %}