<style>

    .dropdown-item{
        color:black;
    }

    .dropdown-divider{
        color:orange;
    }

    .nav-link{
        color:black !important;
        font-size:1.1em;
        font-weight:bold;
    }

</style>

<nav class="navbar navbar-expand-lg navbar-light bg-light pt-1 pb-0">
    
    {% if presentation.creator is defined %}

        {% if app.user and app.user == presentation.creator %}

            <button type="button" id="sidebarCollapse" class="btn mx-md-3 ">
                <i class="fas fa-align-left"></i>
                <span>><</span>
            </button>

        {% endif %}

    {% endif %}

    <!-- Website Logo -  The Project of Projects Logo -->
    
    <a class="navbar-brand ml-3 mr-4 mb-lg-n2 pb-0" href="{{ path('homepage') }}"><img src="/images/app/logo.svg" width="37px" height="44px" style="width:100%;"></a>
        
        
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse ml-5 py-1 my-1 text-body" id="navbarColor03">

        <ul class="navbar-nav mr-auto">

            <li class="nav-item mr-3">
                <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
            </li>

            <li class="nav-item mr-3">
                <a class="nav-link" href="{{path('projects_index')}}">Projets</a>
            </li>
 
          <!--   <li class="nav-item">
                <a class="nav-link" href="{{path('projects_groups_index')}}">Groupes de Projets</a>
            </li>  -->

            <form class="form-inline ml-2">
                <a href="{{path('create_presentation')}}">
                    <button class="btn btn-info font-weight-bold" type="button">+ Ajouter un Projet</button>
                </a>
            </form>

        </ul>

        <ul class="navbar-nav ml-auto">

            {% if app.user %}

                <li class="nav-item dropdown">
                    

                    <a class="nav-link dropdown-toggle bg-info text-white font-weight-bold rounded" style="color:white !important;" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                        
                        {% if app.user.imageName %}
                        
                            <img src="{{app.user.imageName}}" class="" alt="{{app.user.name}}" style="width:25px; height:25px;">

                        {% else %}

                             Compte
                           
                        {% endif %}


                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDopdownLink">

                        <a href="{{ path('create_presentation') }}" class="dropdown-item ">
                           <span class=""></span> Ajouter un Projet</a>

                        <a href="{{ path('account_index') }}" class="dropdown-item">Mes Projets présentés</a>

                        <div class="dropdown-divider"></div>
                        
                        <a href="{{ path('account_profile') }}" class="dropdown-item">Modifier mon profil</a>

                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% else %}
                  
                <li class="nav-item">
                    <a href="{{ path('account_register') }}" class="nav-link btn btn-warning btn-sm mr-2 font-weight-bold">Inscription</a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('account_login') }}" class="nav-link bg-info font-weight-bold rounded" style="color:white !important;">Connexion</a>
                </li>

            {% endif %}
        </ul>
    </div>
    
</nav>