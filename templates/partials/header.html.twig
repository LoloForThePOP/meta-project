<nav class="navbar navbar-expand-lg navbar-light bg-light">

    <button type="button" id="sidebarCollapse" class="btn mx-md-3 ">
        <i class="fas fa-align-left"></i>
        <span>><</span>
    </button>

    <a class="navbar-brand" href="{{ path('homepage') }}">ProtoRoad</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">

            <li class="nav-item">
                <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{path('projects_index')}}">Projets</a>
            </li>

            <form class="form-inline ml-2">
                <a href="{{path('projects_create')}}">
                    <button class="btn btn btn-info font-weight-bold" type="button">+ Ajouter un Projet</button>
                </a>
            </form>

        </ul>

        <ul class="navbar-nav ml-auto">

            {% if app.user %}

                <li class="nav-item dropdown">

                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                        
                        {% if app.user.imageName %}
                        
                            <img src="{{app.user.imageName}}" class="" alt="{{app.user.name}}" style="width:25px; height:25px;">

                        {% else %}

                            <button class="btn btn-primary font-weight-bold">Compte</button>

                        {% endif %}


                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDopdownLink">
                        <a href="{{ path('projects_create') }}" class="dropdown-item">Ajouter un Projet</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_index') }}" class="dropdown-item">Mes Infos</a>
                        <a href="{{ path('account_profile') }}" class="dropdown-item">Modifier le Profil</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% else %}

                <form class="form-inline ml-2">
                   <!--  <a href="{{ path("account_register") }}">
                        <button class="btn btn btn-success font-weight-bold mr-2" type="button">Inscription</button>
                    </a> -->
                    <a href="{{ path('account_login') }}">
                        <button class="btn btn btn-primary font-weight-bold" type="button">Connexion</button>
                    </a>
                </form>

            {% endif %}
        </ul>
    </div>
    
</nav>