{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block stylesheets %}<link rel="stylesheet" href="/css/app_forms.css" />{% endblock %}

{% block title %}Sites Web du Projet{% endblock %}

{% block sidebar %}{% include 'partials/sidebar.html.twig' %}{% endblock %}


{% block body %}
    
    <!-- Label & Input Styles -->

    <style>
                
        input {
            color:black !important;
            font-size:1.25em !important;
            width: 100%;
            padding: 5px 12px;
            margin: 4px 0 8px 0;
            border-style:solid;
            display: inline-block;
            box-sizing: border-box;
        }

        label {
            margin: 0;
        }

    </style>

    <!-- Content Container -->

    <div class="container">

        <!-- Title with Info Button Container -->

        <div class="mt-3 mt-md-4 mb-2">

            <!-- ui Page Title -->

            <h3 class="font-weight-bold d-inline mr-3">Gérer les sites web</h3>

            
            <!-- collapsable advice trigger button -->	

            <a class="font-weight-bold my-1" type="button" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">

                <span class="btn btn-light fs-15 h5 sharp p-1 px-3" style="letter-spacing: 2px;">Conseil</span>
                
            </a>

        </div>

        <!-- Collapsible Info Container -->

        <div class="info-box collapse col-md-7 mt-0 mb-4" id="collapseExample">

            <p class="h5">

                Vous pouvez lister ici tous les sites webs en liens avec votre projet. 

            </p>

            <p class="h5">

                Exemples : site web personnel; réseaux sociaux; serveur Discord; dépôt Github; Trello; Slack et autres.

            </p>

        </div>

        <!-- Add Website Button -->

        <div class="mb-4">

            <button type="button" data-toggle="modal" data-target="#websiteFormModal" class="btn btn-lg btn-success bg-green sharp p-3 font-weight-bold mr-1">+ Ajouter un site web</button> 

        </div>

        <!-- Form Display (in a Modal Box) -->

        <div class="modal fade" id="websiteFormModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">

            <div class="modal-dialog" role="document">

                <div class="modal-content">

                    <div class="modal-header">

                        <h5 class="modal-title" id="exampleModalLabel">Ajouter un site web</h5>

                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>

                    </div>

                    <div class="modal-body">

                        {{ form_start(form) }}

                        {{ form_widget(form) }}

                    </div>

                    <div class="modal-footer">

                        <button type="button" class="btn btn-info sharp font-weight-bold" data-dismiss="modal">Retour</button>         

                        <button class="btn btn-success sharp bg-green font-weight-bold">Valider</button>
                        
                    </div>

                    {{ form_end(form) }}

                </div>

            </div>

        </div>

        <!-- Websites List -->

        <div class="container mb-2">

            <div id="websitesList" class="row col-md-10 pl-0">

                {% if presentation.websites | length > 0 %}

                    {% for website in presentation.websites %}

                        <!-- Thumbnail Container -->

                        <div id="{{website.id}}" class="fs-15 col-md-5 mr-md-4 js-sortableGridItem d-flex align-items-center position-relative mb-3 p-3 border border-primary overflow-hidden cursor-pointer">

                            <a href="{{path('edit_website', {'slug':slug,'idWebsite' : website.id })}}" class="w-100 h-100 text-body text-decoration-none">
                                

                                <span class="font-weight-bold">

                                    {{ website.description }}

                                </span>

                                <br>

                                {{ website.url }}

                            </a>
     
                            <!-- Remove Website Button (Red Cross) -->
            
                            <button type="button" class="js-delete-website d-inline position-absolute btn btn-danger sharp" data-id="{{website.id}}" style="top:0; right:0;"><span class="font-weight-bold">X</span></button>

                        </div>

                    {% endfor %}

                {% endif %}

            </div>

        </div>

        <!-- Back Button -->

        <div>

            <a class="text-decoration-none text-white" href="{{ path('edit_presentation_menu', {'slug': presentation.slug }) }}">
                <button type="button" class="btn btn-info btn-lg font-weight-bold mt-2 mb-4 sharp">Retour</button>
            </a>

        </div>

    </div>

{% endblock %}


{% block javascripts %}

    <!-- sortable plugin in order to reorder slides -->

    <script src="/js/sortable/sortable.min.js"></script>

    <!-- jQuerySupport for this sortable plugin -->
    
    <script src="/js/sortable/jquery-sortable.js"></script>
       
    <script>

        $(document).ready(function(){

            $('#websitesList').sortable({

                animation: 150,

                ghostClass: 'blue-background-class',

                filter: ".disabled",

                onMove: function (evt) {
                    return evt.related.className.indexOf('disabled') === -1;
                },

                // drag and drop implies websites positions updates ajax call

                onEnd: function (evt) {

                    // an array storing slides id position

                    var websitesPosition = [];

                    
                    $('#websitesList div.js-sortableGridItem').each(function(index){
                        
                        websitesPosition.push($(this).attr('id'));
                   
                    });

                    jsonWebsitesPosition = JSON.stringify(websitesPosition);
                    
                    $.ajax({  
                        url: "{{path('ajax_reorder_websites', {'slug': presentation.slug})}}",
                        type:       'POST',   
                        dataType:   'json',
                        data: {
                            "jsonWebsitesPosition": jsonWebsitesPosition,
                        },
    
                        async: true,  
                        
                        success: function(data, status) {
                                 
                        },  
    
                        error : function(xhr, textStatus, errorThrown) {  
                            // alert('Ajax request failed.');  
                        }  
                     }); 

                },
            });
                        
            
            // Remove a Website

            $('#websitesList').on('click', '.js-delete-website', function (){

                var idWebsite = $(this).attr("data-id");

                if (confirm("Confimez vous enlever ce site web ?"))
                {

                $(this).html('<div class="loader"></div>');
                    
                $.ajax({  

                    url: "{{path('ajax_remove_website', {'slug': presentation.slug}) }}",
                    type:       'POST',   
                    dataType:   'json',
                    
                    data: {
                        "idWebsite": idWebsite,
                    },

                    async: true,  
                    
                    success: function(data, status) {


                        $("#websitesList #"+idWebsite).remove();
                        
                    },  

                    error : function(xhr, textStatus, errorThrown) {  
                        // alert('Ajax request failed.');  
                    }  
                    }); 
                    
                }
    
            });

        }); 

        
    </script>
   


{% endblock %}